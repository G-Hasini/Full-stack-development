import java.util.Arrays;

class Main {
    public static void main(String args[]) {

        int[] original = {1, 2, 3};
        int newValue = 4;

        int[] newArray = Arrays.copyOf(original, original.length + 1);

        newArray[newArray.length - 1] = newValue;

        System.out.println(newArray[3]);

        for (int i = 0; i < newArray.length; i++) {
            System.out.println(newArray[i]);
        }
    }
}

// freinds list of array introducing new value
import java.util.Arrays;

class Main {
    public static void main(String args[]) {

        String[] original = {"Hasini", "Farvin", "Thanuja"};
        String newValue = "Poornima";

        String[] newArray = Arrays.copyOf(original, original.length + 1);

        newArray[newArray.length - 1] = newValue;

        System.out.println(newArray[3]);

        for (int i = 0; i < newArray.length; i++) {
            System.out.println(newArray[i]);
        }
    }
}

